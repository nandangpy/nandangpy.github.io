title: Nandang Prayogi - Web Security & Back End Developer
author: Nandang Prayogi
email: contact.nandang@gmail.com
description: > # this means to ignore newlines until "show_exerpts:"
 Personal blog and portfolio of Nandang Prayogi. Articles on Web Security, Penetration Testing, Hacking & CTF.
show_excerpts: true

sass:
  sass_dir: _sass
permalink: /:year/:month/:day/:title 
plainwhite:
  name: Nandang Prayogi
  tagline: Web Developer | Security Engineer
  date_format: "%b %-d, %Y"
  sitemap: true # set to true to generate sitemap.xml content
  # search: true # set to true to enable searchbar
  dark_mode: true # set to true to add dark mode toggle
  portfolio_image: "assets/pp.png" # the path from the base directory of the site to the image to display (no / at the start)
  html_lang: "en" # set the lang attribute of the <html> tag for the pages.
  condensed_mobile:
    #- home
    - page
    - post
  # generate sosmed
  social_links:
    twitter: gakpunyatwitter
    github:  nandangpy
    linkedIn: in/nandang-prayogi
    instagram: nandangpy
    rss: /feed.xml

    # Mastodon instances
    # mastodon:
    # - username: jekyll
    #   instance: example.com
    # - username: jekyll2
    #   instance: example.com
#header_pages:
# - about.md

# Build set
# theme: plainwhite
plugins:
  - jekyll-seo-tag
  - jekyll-feed
  - jekyll-sitemap
domain: nandangpy.github.io       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https://nandangpy.github.io  # the base hostname and protocol for your site, e.g. http://example.com
# baseurl: /nandangpy.github.io/

